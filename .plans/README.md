# AgentForge Plans

Implementation plans are durable project artifacts.

A plan status is one of `Draft`, `Approved`, or `Complete`.

Implementation requires an Approved plan already committed in `HEAD`.
Plan approval and implementation must not share the same commit.
